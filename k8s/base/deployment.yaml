apiVersion: apps/v1
kind: Deployment
metadata:
  name: ciyex-portal-ui
  labels:
    app: ciyex-portal-ui
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ciyex-portal-ui
  template:
    metadata:
      labels:
        app: ciyex-portal-ui
    spec:
      containers:
        - name: ciyex-portal-ui
          image: ciyex-portal-ui:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
          livenessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 30
            periodSeconds: 20
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 3
          env:
            - name: NODE_ENV
              value: "production"

apiVersion: apps/v1
kind: Deployment
metadata:
  name: ciyex-portal-ui
spec:
  template:
    spec:
      containers:
        - name: ciyex-portal-ui
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "stage"
            - name: NEXT_PUBLIC_ENV
              value: "staging"
